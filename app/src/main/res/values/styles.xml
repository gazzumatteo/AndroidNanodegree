<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Button">
        <item name="android:minWidth">@dimen/main_btn_width</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:layout_margin">@dimen/main_btn_margin</item>
    </style>

    <style name="ButtonUdacious" parent="Button">
        <item name="android:background">@color/btn_udaciuos</item>
    </style>
    <style name="ButtonUdaciousAccented" parent="Button">
        <item name="android:textColor">@color/txt_light</item>
        <item name="android:background">@color/btn_accent</item>
    </style>
</resources>
